# gitbits

Git configuration: `git-config` (aliases, defaults) and `gitignore_global`.

## Setup

TODO: document how these are wired up (typically symlinked to `~/.gitconfig` and `~/.gitignore_global`).
