# scriptybits

Standalone utility shell scripts:

- `colors.sh` - shell color variable definitions
- `mkicns.sh` - generate a macOS `.icns` icon from a PNG
- `syncLRMasterToBackup.sh` - Lightroom master → backup sync

## Setup

TODO: document how these are wired up (typically run directly or symlinked into `$PATH`).
